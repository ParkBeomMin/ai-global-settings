---
name: tailwind
description: Using TailwindCSS in Remotion
---

TailwindCSS can be used in Remotion if installed in the project.

**FORBIDDEN:** `transition-*` and `animate-*` classes — always animate via `useCurrentFrame()`.

For setup instructions, see https://www.remotion.dev/docs/tailwind
